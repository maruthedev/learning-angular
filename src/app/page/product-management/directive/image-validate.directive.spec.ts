import { ImageValidateDirective } from './image-validate.directive';

describe('ImageValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageValidateDirective();
    expect(directive).toBeTruthy();
  });
});
