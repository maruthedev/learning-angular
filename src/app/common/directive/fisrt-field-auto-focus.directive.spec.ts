import { FisrtFieldAutoFocusDirective } from './fisrt-field-auto-focus.directive';

describe('FisrtFieldAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FisrtFieldAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
