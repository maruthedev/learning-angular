import { TelValidateDirective } from "./tel-validate.directive";


describe('TelValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new TelValidateDirective();
    expect(directive).toBeTruthy();
  });
});
